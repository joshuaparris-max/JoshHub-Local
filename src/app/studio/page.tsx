import { StudioClient } from "./studio-client";

export const metadata = {
  title: "JoshHub | Studio",
  description: "Creative playground for ideas, stories, and joy logs.",
};

export default function StudioPage() {
  return <StudioClient />;
}
