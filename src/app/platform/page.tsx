import PlatformClient from "./platform-client";

export const metadata = {
    title: "JoshHub | Platform",
    description: "Platform overview.",
};

export default function PlatformPage() {
    return <PlatformClient />;
}
